{
  "name": "tree-sitter-hugo",
  "version": "1.0.0",
  "main": "parser.c",
  "scripts": {
    "build": "gcc -c parser.c -o parser.o && gcc -c grammar.c -o grammar.o && gcc parser.o grammar.o -o
tree-sitter-hugo"
  },
  "dependencies": {
    "tree-sitter-html": "^1.0.0",
    "tree-sitter-go-template": "^1.0.0"
  }
}
